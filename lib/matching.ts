export async function matchProviderToJob(jobId: string) {
  // Placeholder matching logic
  console.log(`Matching providers for job ${jobId}`)
  return { matchedProviderId: 'provider-1', confidence: 0.85 }
}
