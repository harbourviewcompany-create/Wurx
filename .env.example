# Supabase (Project Settings -> API)
NEXT_PUBLIC_SUPABASE_URL=https://rzdavbuoisckvdapbcbj.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only. Never expose to the browser. Used by lib/supabase/server.ts's
# createServiceRoleClient() for trusted server-side work only.
SUPABASE_SERVICE_ROLE_KEY=

# Stripe price IDs for each subscription plan (Stripe Dashboard -> Product catalog)
NEXT_PUBLIC_STRIPE_PRICE_STARTER=
NEXT_PUBLIC_STRIPE_PRICE_PLUS=
NEXT_PUBLIC_STRIPE_PRICE_PRO=

# Used by Stripe checkout success/cancel redirects
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Supabase Edge Function secrets (set via `supabase secrets set`, not here) ---
# STRIPE_SECRET_KEY
# STRIPE_WEBHOOK_SIGNING_SECRET
# SUPABASE_URL                 (auto-provided in Edge Function runtime)
# SUPABASE_SERVICE_ROLE_KEY    (auto-provided in Edge Function runtime)
# SITE_URL
